version: "3.8"

services:
  frontend:
    container_name: perfume-frontend
    build:
      context: .
      args:
        NEXT_PUBLIC_STRAPI_URL: https://kioskapi.gandom-perfume.ir
        STRAPI_TOKEN: your-private-token-here
    env_file:
      - .env
    ports:
      - "3000:3000" # ✅ run on port 80 for production
    restart: always

# Base URL for the Strapi instance. This can be public because it only contains the domain.
NEXT_PUBLIC_STRAPI_URL=https://kioskapi.gandom-perfume.ir

# Private Strapi access token used by the Next.js server and API routes. Do NOT expose this
# value via a NEXT_PUBLIC_ prefix or commit it to version control.
STRAPI_TOKEN=
